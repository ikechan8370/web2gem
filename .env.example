# Worker secrets used by the Cloudflare Deploy Button.
# Non-secret Worker configuration is visible in wrangler.jsonc vars.
API_KEYS=

# Account-pool admin auth. Use one strong key. Empty leaves admin auth
# unconfigured; placeholder values such as changeme are rejected.
ADMIN_KEY=
